[murano]
# keystone url
auth_url = http://cloud.lab.fi-ware.org:4731/v3

auth_port = 4731

# Protocol of the admin Identity API endpoint (http or https).
# Deprecated, use identity_uri. (string value)
auth_protocol = http

# Complete public Identity API endpoint (string value)
auth_uri = http://cloud.lab.fiware.org:4731/v3


# API version of the admin Identity API endpoint (string value)
auth_version = v3

password = XXX
user = admin
tenant = admin
region_name = 'Spain2'

# keystone tenant
# tenant = admin

# keyname - used for debugging murano-agent
keyname = otro

# murano url
# murano_url = http://127.0.0.1:8082

# Flavor for sanity checks
standard_flavor = m1.small

# Flavor for advanced checks
# advanced_flavor = m1.small

# image for linux services
linux_image =  base_ubuntu_14.04

conflang_image = base_ubuntu_14.04

instance_type=io.murano.resources.FiwareMuranoInstance


# image for docker applications
# docker_image = debian-8-docker.qcow2

# image for kubernetes applications
# kubernetes_image = ubuntu14.04-x64-kubernetes.qcow2

# image for windows services
# windows_image = default_windows

# image for hdp sandbox
# hdp_image = hdp-sandbox

murano_apps_folder = /Users/henar/Desktop/FIWARE_Software/fiware-enablers/murano-apps/
