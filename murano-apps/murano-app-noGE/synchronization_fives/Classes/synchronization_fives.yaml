Namespaces:
  =: io.murano.conflang.chef
  std: io.murano
  res: io.murano.resources
  sys: io.murano.system


Name: synchronization_fives

Extends: std:Application

Properties:
  instance:
    Contract: $.class(res:Instance).notNull()


Methods:
  initialize:
    Body:
      - $._environment: $.find(std:Environment).require()

  deploy:
    Body:
      - If: not $.getAttr(deployed, false)
        Then:
          - $._environment.reporter.report($this, 'Creating VM for deployment')
          - $securityGroupIngress:
            - ToPort: 22
              FromPort: 22
              IpProtocol: tcp
              External: true
            - ToPort: 8181
              FromPort: 8181
              IpProtocol: tcp
              External: true
            - ToPort: 8081
              FromPort: 8081
              IpProtocol: tcp
              External: true
            - ToPort: 34837
              FromPort: 34837
              IpProtocol: tcp
              External: true

          - $._environment.securityGroupManager.addGroupIngress($securityGroupIngress)
          - $.instance.addNid()
          - $.instance.deploy()
          - $resources: new(sys:Resources)
          # Deploy synchronization_fives
          - $template: $resources.yaml('Deploysynchronization_fives.template')
          - $._environment.reporter.report($this, 'Instance is created. Deploying synchronization_fives')
          - $.instance.agent.call($template, $resources)
          - If: $.instance.assignFloatingIp
            Then:
              - $host: $.instance.floatingIpAddress
            Else:
              - $host: $.instance.ipAddresses[0]
          - $._environment.reporter.report($this, format('synchronization_fives is installed at {0}', $host))
          - $.setAttr(deployed, true)
